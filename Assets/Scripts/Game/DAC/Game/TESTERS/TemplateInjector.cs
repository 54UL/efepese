using UnityEngine;

public class TemplateInjector : MonoBehaviour {

    //public DAC.IInputManager InputManagerService;
    void Start()
    {
        //InputManagerService = (DAC.IInputManager) ServiceInjector.getSingleton(typeof(DAC.InputManager));
        //InputManagerService = (DAC.IInputManager)ServiceInjector.Inject("InputManagerService");

        //InputManagerService.GetAllControls();
    }
}